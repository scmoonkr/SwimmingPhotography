{
  "name": "swimming-photography",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "dashboard"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client",
    "dashboard": "npm run dev --workspace=dashboard",
    "dev": "npm run server & npm run client & npm run dashboard"
  }
}
